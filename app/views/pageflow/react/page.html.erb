<% if page.template.present? %>
  <% concat React::ServerRendering.render(
     Pageflow.config.page_types.find_by_name!(page.template).component_name, @p ||= {
     resolverSeed: entry_seed(@entry),
     pageId: page.perma_id
   }.to_json, true) %>
<% end %>
